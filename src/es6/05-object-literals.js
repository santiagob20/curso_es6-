// Enhanced object literals

function newUer(iser, age, country) {
  return {
    user,
    age,
    country
  }
}

// si es assitente se debe mostrar
// - un nuevo menu debajo de grupos (llamado assited users) que tenga un select (como en cards) multichips
// en la que se pueda seleccionar un user de la operation
// ese select va a actualizasr un campo de user llamado assisted_users solo por el ID de los users

// agergar un select en el user que muestre los assistant users  del usuario actual (disabled)